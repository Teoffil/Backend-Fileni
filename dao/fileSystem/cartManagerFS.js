// cartManagerFS.js

const fs = require('fs');

class CartManagerFS {

    saveCart(cart) {
        // escribe el carrito en archivo carts.json
    }

    getCarts() {
    // lee los carritos del archivo carts.json
    }

  // etc...
}

module.exports = CartManagerFS;